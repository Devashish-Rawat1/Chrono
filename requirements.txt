# Chrono — dependencies
# Install with: pip install -r requirements.txt

# Google Calendar + Tasks API
google-auth>=2.40.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.180.0

# Groq API calls (Task Analysis Agent)
requests>=2.32.0

# .env loading (main.py)
python-dotenv>=1.1.0

# Multi-agent orchestration (used once agent pipeline is wired together)
agno==2.6.9

# Local LLM fallback (Qwen3 8B via Ollama) — uncomment once that piece is built
# ollama>=0.6.0

# Google Sheets output (planner) — uncomment once Output 2 is built
# gspread>=6.2.0
